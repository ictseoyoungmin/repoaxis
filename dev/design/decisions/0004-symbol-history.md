# Decision 0004 — File history is exact before symbol history

Git natively versions files and lines, not stable function identities. File-level commit context can be exact. Symbol-level historical identity requires inference and must not be presented as exact until a separate confidence-bearing mechanism exists.
