import fs from 'node:fs';

function replaceOnce(source, needle, replacement, label) {
  if (!source.includes(needle)) throw new Error(`missing ${label}`);
  return source.replace(needle, replacement);
}

const viewerPath = 'skills/repoaxis/viewer/viewer-3.js';
let viewer = fs.readFileSync(viewerPath, 'utf8');
viewer = replaceOnce(viewer,
`function graphEdge(A,B){const dx=B[0]-A[0],dir=dx>=0?1:-1,sx=A[0]+dir*70,tx=B[0]-dir*70,m=(sx+tx)/2;return\`M \${sx} \${A[1]} C \${m} \${A[1]},\${m} \${B[1]},\${tx} \${B[1]}\`}\n`,
`function graphEdge(A,B){const dx=B[0]-A[0],dir=dx>=0?1:-1,sx=A[0]+dir*70,tx=B[0]-dir*70,m=(sx+tx)/2;return\`M \${sx} \${A[1]} C \${m} \${A[1]},\${m} \${B[1]},\${tx} \${B[1]}\`}\nfunction graphScopeKey(f){return f?.repoPath?.includes('/')?f.repoPath.split('/')[0]:'root'}\nfunction graphExploreContext(id,visible){\n  const inbound=[],outbound=[],neighbors=new Set([id]),scopes=new Set,shown=visible instanceof Set?visible:new Set(visible||[]);\n  for(const[a,b]of edges){if(!shown.has(a)||!shown.has(b))continue;if(b===id){inbound.push(a);neighbors.add(a)}if(a===id){outbound.push(b);neighbors.add(b)}}\n  for(const nodeId of neighbors){const f=nodes[nodeId];if(f)scopes.add(graphScopeKey(f))}\n  return{inbound,outbound,neighbors,scopes}\n}\nfunction clearGraphExploration(){const svg=$('#graphSvg'),ctx=$('#graphHoverContext');if(!svg)return;svg.classList.remove('exploring');svg.querySelectorAll('.graph-active,.graph-neighbor,.graph-in,.graph-out,.graph-related').forEach(el=>el.classList.remove('graph-active','graph-neighbor','graph-in','graph-out','graph-related'));if(ctx){ctx.hidden=true;ctx.textContent=''}}\nfunction showGraphExploration(id,visible){\n  const svg=$('#graphSvg'),ctx=$('#graphHoverContext'),f=nodes[id];if(!svg||!f)return;const c=graphExploreContext(id,visible);clearGraphExploration();svg.classList.add('exploring');\n  svg.querySelector(\`.node[data-id="\${CSS.escape(id)}"]\`)?.classList.add('graph-active');\n  for(const n of c.neighbors)if(n!==id)svg.querySelector(\`.node[data-id="\${CSS.escape(n)}"]\`)?.classList.add('graph-neighbor');\n  for(const a of c.inbound)svg.querySelector(\`.edge[data-from="\${CSS.escape(a)}"][data-to="\${CSS.escape(id)}"]\`)?.classList.add('graph-in');\n  for(const b of c.outbound)svg.querySelector(\`.edge[data-from="\${CSS.escape(id)}"][data-to="\${CSS.escape(b)}"]\`)?.classList.add('graph-out');\n  for(const s of c.scopes)svg.querySelector(\`.graph-scope[data-scope="\${CSS.escape(s)}"]\`)?.classList.add('graph-related');\n  if(ctx){ctx.hidden=false;ctx.textContent=\`\${f.label} · \${c.inbound.length} in · \${c.outbound.length} out\`}\n}\nfunction bindGraphExploration(projection,visible){clearGraphExploration();if(projection.mode==='impact')return;const svg=$('#graphSvg');svg.querySelectorAll('.node[data-id]').forEach(el=>{el.addEventListener('mouseenter',()=>showGraphExploration(el.dataset.id,visible));el.addEventListener('mouseleave',clearGraphExploration)})}\n`, 'graph exploration helpers');
viewer = replaceOnce(viewer,
`for(const s of L.scopes)h+=\`<g><rect class="folder-scope" x="\${s.x}" y="\${s.y}" width="\${s.w}" height="\${s.h}" rx="18"/><text class="scope-label" x="\${s.x+14}" y="\${s.y+20}">\${esc(s.name==='root'?'root/':s.name+'/')}</text></g>\`;`,
`for(const s of L.scopes)h+=\`<g class="graph-scope" data-scope="\${esc(s.name)}"><rect class="folder-scope" x="\${s.x}" y="\${s.y}" width="\${s.w}" height="\${s.h}" rx="18"/><text class="scope-label" x="\${s.x+14}" y="\${s.y+20}">\${esc(s.name==='root'?'root/':s.name+'/')}</text></g>\`;`, 'graph scope metadata');
viewer = replaceOnce(viewer,
`h+=\`<path marker-end="url(#graphArrow)" class="edge \${rel==='in'?'hot':''} \${dim?'dim':''}" data-edge="\${esc(a+'>'+b)}" d="\${graphEdge(A,B)}" style="\${hit?'stroke:#625bff;stroke-width:2':''}"/>\``,
`h+=\`<path marker-end="url(#graphArrow)" class="edge \${rel==='in'?'hot':''} \${dim?'dim':''}" data-edge="\${esc(a+'>'+b)}" data-from="\${esc(a)}" data-to="\${esc(b)}" d="\${graphEdge(A,B)}" style="\${hit?'stroke:#625bff;stroke-width:2':''}"/>\``, 'graph edge metadata');
viewer = replaceOnce(viewer,
`renderGraphNotice(projection,sel,visible);bindNodes(svg);applyCamera();applyFilter();bindImpactTrace()}`,
`renderGraphNotice(projection,sel,visible);bindNodes(svg);bindGraphExploration(projection,visible);applyCamera();applyFilter();bindImpactTrace()}`, 'graph exploration binding');
fs.writeFileSync(viewerPath, viewer);

const htmlPath='skills/repoaxis/viewer/repoaxis.html';let html=fs.readFileSync(htmlPath,'utf8');
html=replaceOnce(html,`<span class="mode-label" id="graphScope">Whole repository · canonical file imports</span><button class="mini" id="graphNeighborhood">`,`<span class="mode-label" id="graphScope">Whole repository · canonical file imports</span><span class="mode-label graph-hover-context" id="graphHoverContext" hidden></span><button class="mini" id="graphNeighborhood">`,'graph hover context host');
fs.writeFileSync(htmlPath,html);

const cssPath='skills/repoaxis/viewer/viewer-base.css';let css=fs.readFileSync(cssPath,'utf8');
css += `\n.graph-hover-context{color:#404858;font-weight:650;max-width:240px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.graph-svg .node,.graph-svg .edge,.graph-svg .graph-scope{transition:opacity .12s,stroke .12s,fill .12s}.graph-svg.exploring .node{opacity:.16}.graph-svg.exploring .edge{opacity:.07}.graph-svg.exploring .graph-scope{opacity:.18}.graph-svg.exploring .node.graph-active,.graph-svg.exploring .node.graph-neighbor,.graph-svg.exploring .edge.graph-in,.graph-svg.exploring .edge.graph-out,.graph-svg.exploring .graph-scope.graph-related{opacity:1}.graph-svg .edge.graph-in{stroke:#7b74f4!important;stroke-width:2!important}.graph-svg .edge.graph-out{stroke:#0d9488!important;stroke-width:2!important}.graph-svg .node.graph-active .bg{stroke:#625bff!important;stroke-width:2!important;fill:#fbfaff!important}.graph-svg .node.graph-neighbor .bg{stroke:#bbb7f7}.graph-svg .graph-scope.graph-related .folder-scope{stroke:#d7d3ff;fill:rgba(248,247,255,.82)}\n`;
fs.writeFileSync(cssPath,css);

fs.writeFileSync('dev/tests/integration/viewer-graph-exploration.test.mjs',`import assert from "node:assert/strict";\nimport fs from "node:fs";\nimport test from "node:test";\nimport vm from "node:vm";\nimport { fileURLToPath } from "node:url";\nconst VIEWER=fileURLToPath(new URL("../../../skills/repoaxis/viewer/viewer-3.js",import.meta.url));\nconst source=fs.readFileSync(VIEWER,"utf8"),start=source.indexOf("function graphScopeKey"),end=source.indexOf("function renderGraphNotice"),slice=source.slice(start,end);\nfunction makeContext(){const nodes={"file:src/a.js":{id:"file:src/a.js",repoPath:"src/a.js",label:"a.js"},"file:src/b.js":{id:"file:src/b.js",repoPath:"src/b.js",label:"b.js"},"file:test/c.js":{id:"file:test/c.js",repoPath:"test/c.js",label:"c.js"},"file:other/d.js":{id:"file:other/d.js",repoPath:"other/d.js",label:"d.js"}},edges=[["file:src/b.js","file:src/a.js"],["file:src/a.js","file:test/c.js"],["file:other/d.js","file:src/a.js"]],context=vm.createContext({nodes,edges,$:()=>null,CSS:{escape:String}});vm.runInContext(slice,context);return context}\ntest("hover context derives visible incoming and outgoing neighbors",()=>{const c=makeContext(),x=c.graphExploreContext("file:src/a.js",new Set(["file:src/a.js","file:src/b.js","file:test/c.js"]));assert.deepEqual([...x.inbound],["file:src/b.js"]);assert.deepEqual([...x.outbound],["file:test/c.js"]);assert.deepEqual([...x.neighbors].sort(),["file:src/a.js","file:src/b.js","file:test/c.js"].sort())});\ntest("hover context keeps only participating visible scopes",()=>{const c=makeContext(),x=c.graphExploreContext("file:src/a.js",new Set(["file:src/a.js","file:src/b.js","file:test/c.js"]));assert.deepEqual([...x.scopes].sort(),["src","test"])});\ntest("graph rendering exposes directional edge and scope metadata",()=>{assert.match(source,/data-from=/);assert.match(source,/data-to=/);assert.match(source,/class=\\"graph-scope\\" data-scope=/);assert.match(source,/bindGraphExploration\\(projection,visible\\)/)});\n`);
