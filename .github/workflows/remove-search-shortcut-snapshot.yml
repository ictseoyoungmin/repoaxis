name: remove-search-shortcut-snapshot

on:
  push:
    branches:
      - fix/remove-search-shortcut-badge

permissions:
  contents: read

jobs:
  capture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run check
      - run: npm install --no-save playwright@1.55.0
      - run: npx playwright install --with-deps chromium
      - run: node bin/repoaxis build
      - name: Start viewer
        run: |
          node bin/repoaxis view --no-open --port 4173 > /tmp/repoaxis-viewer.log 2>&1 &
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:4173/api/health >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/repoaxis-viewer.log
          exit 1
      - run: node dev/tools/capture-search-chrome.mjs
      - run: node bin/repoaxis snapshot --output search-chrome-without-shortcut.html
      - uses: actions/upload-artifact@v4
        with:
          name: search-chrome-without-shortcut
          path: |
            search-chrome-without-shortcut.png
            search-chrome-without-shortcut.html
          retention-days: 7
