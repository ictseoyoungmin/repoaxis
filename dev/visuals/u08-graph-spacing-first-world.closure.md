# U08 closure boundary

U08 changes only Graph world spacing and default camera entry/reset behavior. It does not change index/schema data, the 80-file bounded projection, focused/impact semantics, routed-edge geometry, dependency navigation, Git state, snapshot format, or APIs.

The default Graph is intentionally not a whole-world fit. `Fit` remains the explicit whole-projection overview action.
