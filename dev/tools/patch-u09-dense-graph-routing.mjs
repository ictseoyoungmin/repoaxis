import fs from 'node:fs';

const viewerPath='skills/repoaxis/viewer/viewer-3.js';
const testPath='dev/tests/integration/viewer-graph-dense-routing.test.mjs';

let src=fs.readFileSync(viewerPath,'utf8');
const start=src.indexOf('const GRAPH_NODE_GEOMETRY');
const end=src.indexOf('function graphExploreContext',start);
if(start<0||end<0)throw new Error('dense routing anchors not found');
const block=String.raw`const GRAPH_NODE_GEOMETRY={halfW:68,halfH:21,gate:20};
let graphRoutingState={used:[]};
function graphPreferredSides(A,B){const dx=B[0]-A[0],dy=B[1]-A[1],ax=Math.abs(dx),ay=Math.abs(dy);if(ax>=ay*.82)return[dx>=0?'R':'L',dx>=0?'L':'R'];return[dy>=0?'B':'T',dy>=0?'T':'B']}
function graphSidePort(P,side,offset=0){const g=GRAPH_NODE_GEOMETRY,{halfW:w,halfH:h,gate}=g;if(side==='L')return{base:[P[0]-w,P[1]+offset],gate:[P[0]-w-gate,P[1]+offset],side};if(side==='R')return{base:[P[0]+w,P[1]+offset],gate:[P[0]+w+gate,P[1]+offset],side};if(side==='T')return{base:[P[0]+offset,P[1]-h],gate:[P[0]+offset,P[1]-h-gate],side};return{base:[P[0]+offset,P[1]+h],gate:[P[0]+offset,P[1]+h+gate],side}}
function graphAdvancePoint(P,side,d=28){if(side==='L')return[P[0]-d,P[1]];if(side==='R')return[P[0]+d,P[1]];if(side==='T')return[P[0],P[1]-d];return[P[0],P[1]+d]}
function graphSimplifyRoute(points){const out=[];for(const p of points){if(!p||!Number.isFinite(p[0])||!Number.isFinite(p[1]))continue;const q=out.at(-1);if(q&&Math.abs(q[0]-p[0])<.01&&Math.abs(q[1]-p[1])<.01)continue;out.push([p[0],p[1]])}let changed=true;while(changed&&out.length>2){changed=false;for(let i=1;i<out.length-1;i++){const a=out[i-1],b=out[i],c=out[i+1],sameX=Math.abs(a[0]-b[0])<.01&&Math.abs(b[0]-c[0])<.01,sameY=Math.abs(a[1]-b[1])<.01&&Math.abs(b[1]-c[1])<.01;if(sameX||sameY){out.splice(i,1);changed=true;break}}}return out}
function graphRoundedRoute(points,r=10){const p=graphSimplifyRoute(points);if(p.length<2)return'';let d=\`M \${p[0][0]} \${p[0][1]}\`;for(let i=1;i<p.length;i++){if(i===p.length-1){d+=\` L \${p[i][0]} \${p[i][1]}\`;continue}const a=p[i-1],b=p[i],c=p[i+1],v1=[a[0]-b[0],a[1]-b[1]],v2=[c[0]-b[0],c[1]-b[1]],l1=Math.max(1,Math.hypot(...v1)),l2=Math.max(1,Math.hypot(...v2)),rr=Math.min(r,l1/2,l2/2),s=[b[0]+v1[0]/l1*rr,b[1]+v1[1]/l1*rr],e=[b[0]+v2[0]/l2*rr,b[1]+v2[1]/l2*rr];d+=\` L \${s[0]} \${s[1]} Q \${b[0]} \${b[1]} \${e[0]} \${e[1]}\`}return d}
function graphScopeMap(L){return new Map(L.scopes.map(s=>[s.name,s]))}
function graphPortPlan(visible,L){const records=[],groups=new Map;for(const[a,b]of edges){if(!visible.has(a)||!visible.has(b)||!L.pos[a]||!L.pos[b])continue;const[ss,ts]=graphPreferredSides(L.pos[a],L.pos[b]),rec={a,b,ss,ts,so:0,to:0,sc:1,tc:1};records.push(rec);for(const [role,id,side,other] of [['s',a,ss,b],['t',b,ts,a]]){const key=id+':'+side;if(!groups.has(key))groups.set(key,[]);groups.get(key).push({rec,role,other,side})}}for(const items of groups.values()){items.sort((x,y)=>{const px=L.pos[x.other],py=L.pos[y.other],side=x.side;return(side==='L'||side==='R'?px[1]-py[1]:px[0]-py[0])||(x.role.localeCompare(y.role))});const n=items.length,side=items[0]?.side,maxSpan=side==='L'||side==='R'?Math.max(0,(GRAPH_NODE_GEOMETRY.halfH-5)*2):Math.max(0,(GRAPH_NODE_GEOMETRY.halfW-12)*2),step=n<=1?0:Math.min(7,maxSpan/(n-1));items.forEach((item,i)=>{const off=(i-(n-1)/2)*step;if(item.role==='s'){item.rec.so=off;item.rec.sc=n}else{item.rec.to=off;item.rec.tc=n}})}return new Map(records.map(r=>[r.a+'>'+r.b,r]))}
function graphScopeKey(f){return f?.repoPath?.includes('/')?f.repoPath.split('/')[0]:'root'}
function graphRouteObstacles(L,a,b,pad=12){const g=GRAPH_NODE_GEOMETRY,out=[];for(const[id,p]of Object.entries(L.pos)){if(id===a||id===b)continue;out.push({id,x1:p[0]-g.halfW-pad,x2:p[0]+g.halfW+pad,y1:p[1]-g.halfH-pad,y2:p[1]+g.halfH+pad})}return out}
function graphSegmentBlocked(A,B,obs){const eps=.01,vertical=Math.abs(A[0]-B[0])<eps,horizontal=Math.abs(A[1]-B[1])<eps;if(!vertical&&!horizontal)return true;if(vertical){const x=A[0],lo=Math.min(A[1],B[1]),hi=Math.max(A[1],B[1]);return obs.some(r=>x>r.x1&&x<r.x2&&hi>r.y1&&lo<r.y2)}const y=A[1],lo=Math.min(A[0],B[0]),hi=Math.max(A[0],B[0]);return obs.some(r=>y>r.y1&&y<r.y2&&hi>r.x1&&lo<r.x2)}
function graphRouteClear(points,obs){const p=graphSimplifyRoute(points);for(let i=1;i<p.length;i++)if(graphSegmentBlocked(p[i-1],p[i],obs))return false;return true}
function graphRouteStats(points){const p=graphSimplifyRoute(points);let length=0,turns=0;for(let i=1;i<p.length;i++)length+=Math.abs(p[i][0]-p[i-1][0])+Math.abs(p[i][1]-p[i-1][1]);for(let i=2;i<p.length;i++){const a=p[i-2],b=p[i-1],c=p[i],v1=Math.abs(a[0]-b[0])<.01,v2=Math.abs(b[0]-c[0])<.01;if(v1!==v2)turns++}return{length,turns}}
function graphRangeOverlap(a1,a2,b1,b2){return Math.max(0,Math.min(Math.max(a1,a2),Math.max(b1,b2))-Math.max(Math.min(a1,a2),Math.min(b1,b2)))}
function graphRouteUsePenalty(points){const p=graphSimplifyRoute(points);let score=0;for(let i=1;i<p.length;i++){const A=p[i-1],B=p[i],v=Math.abs(A[0]-B[0])<.01;for(const [C,D] of graphRoutingState.used){const uv=Math.abs(C[0]-D[0])<.01;if(v===uv){if(v){const overlap=graphRangeOverlap(A[1],B[1],C[1],D[1]),dist=Math.abs(A[0]-C[0]);if(overlap&&dist<1.5)score+=overlap*3.4;else if(overlap&&dist<9)score+=overlap*.65}else{const overlap=graphRangeOverlap(A[0],B[0],C[0],D[0]),dist=Math.abs(A[1]-C[1]);if(overlap&&dist<1.5)score+=overlap*3.4;else if(overlap&&dist<9)score+=overlap*.65}}else if(v){const x=A[0],y=C[1];if(x>Math.min(C[0],D[0])&&x<Math.max(C[0],D[0])&&y>Math.min(A[1],B[1])&&y<Math.max(A[1],B[1]))score+=14}else{const x=C[0],y=A[1];if(x>Math.min(A[0],B[0])&&x<Math.max(A[0],B[0])&&y>Math.min(C[1],D[1])&&y<Math.max(C[1],D[1]))score+=14}}}return score}
function graphLaneValues(axis,S,T,obs,L){const ai=axis==='x'?0:1,mid=(S[ai]+T[ai])/2,vals=[mid,S[ai]+(T[ai]-S[ai])*.35,S[ai]+(T[ai]-S[ai])*.65];for(const r of obs){vals.push(axis==='x'?r.x1-18:r.y1-18,axis==='x'?r.x2+18:r.y2+18)}for(const s of L.scopes||[]){vals.push(axis==='x'?s.x-28:s.y-28,axis==='x'?s.x+s.w+28:s.y+s.h+28)}const max=axis==='x'?L.W:L.H;return [...new Set(vals.map(v=>Math.max(18,Math.min(max-18,Math.round(v*2)/2))))].sort((a,b)=>Math.abs(a-mid)-Math.abs(b-mid)||a-b).slice(0,14)}
function graphPortDirectionOk(points,sp,tp){const p=graphSimplifyRoute(points);if(p.length<2)return false;const S=p[0],N=p[1],P=p.at(-2),T=p.at(-1),okStart=sp.side==='L'?N[0]<=S[0]:sp.side==='R'?N[0]>=S[0]:sp.side==='T'?N[1]<=S[1]:N[1]>=S[1],okEnd=tp.side==='L'?P[0]<=T[0]:tp.side==='R'?P[0]>=T[0]:tp.side==='T'?P[1]<=T[1]:P[1]>=T[1];return okStart&&okEnd}
function graphRouteCandidates(sp,tp,obs,L){const S=sp.gate,T=tp.gate,P=graphAdvancePoint(S,sp.side,30),Q=graphAdvancePoint(T,tp.side,30),xs=graphLaneValues('x',P,Q,obs,L),ys=graphLaneValues('y',P,Q,obs,L),out=[];out.push([S,P,[Q[0],P[1]],Q,T],[S,P,[P[0],Q[1]],Q,T]);for(const y of ys)out.push([S,P,[P[0],y],[Q[0],y],Q,T]);for(const x of xs)out.push([S,P,[x,P[1]],[x,Q[1]],Q,T]);for(let i=0;i<Math.min(6,xs.length,ys.length);i++){const x=xs[i],y=ys[i];out.push([S,P,[x,P[1]],[x,y],[Q[0],y],Q,T],[S,P,[P[0],y],[x,y],[x,Q[1]],Q,T])}return out}
function graphAlternateSide(side,from,to){const dx=to[0]-from[0],dy=to[1]-from[1];return side==='L'||side==='R'?(dy>=0?'B':'T'):(dx>=0?'R':'L')}
function graphBestRouteForSides(a,b,L,ss,ts,so,to,pairPenalty=0){const A=L.pos[a],B=L.pos[b],sp=graphSidePort(A,ss,so),tp=graphSidePort(B,ts,to),obs=graphRouteObstacles(L,a,b),direct=Math.max(1,Math.abs(A[0]-B[0])+Math.abs(A[1]-B[1]));let best=null;for(const raw of graphRouteCandidates(sp,tp,obs,L)){const pts=graphSimplifyRoute(raw);if(!graphPortDirectionOk(pts,sp,tp)||!graphRouteClear(pts,obs))continue;const stats=graphRouteStats(pts),usage=graphRouteUsePenalty(pts);let score=pairPenalty+stats.length+stats.turns*42+usage;if(stats.length>direct*1.55+90)score+=(stats.length-(direct*1.55+90))*2.8;if(!best||score<best.score)best={score,pts,sp,tp,stats,usage}}return best}
function graphRoutedEdge(a,b,L,plan){const A=L.pos[a],B=L.pos[b];if(!A||!B)return null;const pref=plan.get(a+'>'+b)||(()=>{const[ss,ts]=graphPreferredSides(A,B);return{ss,ts,so:0,to:0,sc:1,tc:1}})(),altS=graphAlternateSide(pref.ss,A,B),altT=graphAlternateSide(pref.ts,B,A),saturated=pref.sc>=6||pref.tc>=6,pairs=[[pref.ss,pref.ts,pref.so,pref.to,0],[pref.ss,altT,pref.so,0,saturated?110:190],[altS,pref.ts,0,pref.to,saturated?110:190],[altS,altT,0,0,saturated?220:380]];let best=null;for(const pair of pairs){const r=graphBestRouteForSides(a,b,L,...pair);if(r&&(!best||r.score<best.score))best=r}const sa=graphScopeKey(nodes[a]),ta=graphScopeKey(nodes[b]),same=sa===ta;if(!best){const sp=graphSidePort(A,pref.ss,pref.so),tp=graphSidePort(B,pref.ts,pref.to),x=(sp.gate[0]+tp.gate[0])/2,pts=graphSimplifyRoute([sp.base,sp.gate,[x,sp.gate[1]],[x,tp.gate[1]],tp.gate,tp.base]);return{path:graphRoundedRoute(pts,10),points:pts,kind:same?'local':'cross-folder',sourceSide:pref.ss,targetSide:pref.ts,strategy:'fallback',blocked:true,turns:graphRouteStats(pts).turns}}const pts=graphSimplifyRoute([best.sp.base,...best.pts,best.tp.base]);for(let i=1;i<best.pts.length;i++)graphRoutingState.used.push([best.pts[i-1],best.pts[i]]);return{path:graphRoundedRoute(pts,10),points:pts,kind:same?'local':'cross-folder',sourceSide:best.sp.side,targetSide:best.tp.side,strategy:best.stats.turns>=4?'detour':'orthogonal',blocked:false,turns:best.stats.turns}}
`;
src=src.slice(0,start)+block+src.slice(end);
src=src.replace('portPlan=graphPortPlan(visible,L);if(state.selected===ROOT&&sel)state.selected=sel.id;','portPlan=graphPortPlan(visible,L);graphRoutingState={used:[]};if(state.selected===ROOT&&sel)state.selected=sel.id;');
src=src.replace('data-edge="${key}" data-from="${from}" data-to="${to}" d="${route.path}"/>`;focusEdges+=`<path marker-end="url(#graphArrow)" class="edge graph-edge-focus ${route.kind}', 'data-edge="${key}" data-from="${from}" data-to="${to}" data-route="${route.strategy}" data-obstacle="${route.blocked?\'blocked\':\'clear\'}" d="${route.path}"/>`;focusEdges+=`<path marker-end="url(#graphArrow)" class="edge graph-edge-focus ${route.kind}');
src=src.replace('data-edge="${key}" data-from="${from}" data-to="${to}" d="${route.path}"/>`}h+=', 'data-edge="${key}" data-from="${from}" data-to="${to}" data-route="${route.strategy}" data-obstacle="${route.blocked?\'blocked\':\'clear\'}" d="${route.path}"/>`}h+=');
fs.writeFileSync(viewerPath,src);

const test=String.raw`import assert from 'node:assert/strict';
import fs from 'node:fs';
import test from 'node:test';
import vm from 'node:vm';
import { fileURLToPath } from 'node:url';

const VIEWER=fileURLToPath(new URL('../../../skills/repoaxis/viewer/viewer-3.js',import.meta.url));
const source=fs.readFileSync(VIEWER,'utf8');
const start=source.indexOf('const GRAPH_NODE_GEOMETRY');
const end=source.indexOf('function graphExploreContext',start);
const helpers=source.slice(start,end);
function context({nodes,edges}){const c=vm.createContext({nodes,edges,console});vm.runInContext(helpers,c);return c}
function file(id,path){return{id,type:'file',repoPath:path,label:path.split('/').at(-1)}}

test('dense router detours around an intervening file node',()=>{const nodes={a:file('a','src/a.mjs'),b:file('b','src/b.mjs'),c:file('c','src/c.mjs')},edges=[['a','b']],c=context({nodes,edges}),L={pos:{a:[120,180],c:[360,180],b:[600,180]},scopes:[{name:'src',x:30,y:90,w:660,h:260}],W:760,H:420},plan=c.graphPortPlan(new Set(['a','b','c']),L),route=c.graphRoutedEdge('a','b',L,plan),obs=c.graphRouteObstacles(L,'a','b');assert.ok(route);assert.equal(route.blocked,false);assert.equal(c.graphRouteClear(route.points.slice(1,-1),obs),true);assert.ok(route.turns>=2)});
test('shared physical side ports account for incoming and outgoing crowding together',()=>{const nodes={a:file('a','src/a.mjs'),b:file('b','src/b.mjs'),c:file('c','src/c.mjs'),d:file('d','src/d.mjs')},edges=[['a','b'],['c','a'],['a','d']],c=context({nodes,edges}),L={pos:{a:[300,200],b:[560,150],c:[40,210],d:[560,250]},scopes:[{name:'src',x:0,y:80,w:640,h:300}],W:700,H:460},plan=c.graphPortPlan(new Set(Object.keys(nodes)),L);const ab=plan.get('a>b'),ad=plan.get('a>d');assert.ok(ab.sc>=2);assert.equal(ab.sc,ad.sc);assert.notEqual(ab.so,ad.so)});
test('route reuse is penalized so dense edges can seek alternate corridors',()=>{const nodes={a:file('a','src/a.mjs'),b:file('b','src/b.mjs')},edges=[['a','b']],c=context({nodes,edges}),L={pos:{a:[120,180],b:[600,360]},scopes:[{name:'src',x:30,y:90,w:660,h:380}],W:760,H:520},plan=c.graphPortPlan(new Set(['a','b']),L),route=c.graphRoutedEdge('a','b',L,plan);assert.ok(route);assert.ok(c.graphRoutingState.used.length>0);assert.ok(c.graphRouteUsePenalty(route.points.slice(1,-1))>0)});
test('renderer exposes obstacle status and route strategy for visual QA',()=>{assert.match(source,/data-route=/);assert.match(source,/data-obstacle=/);assert.match(source,/graphRoutingState=\{used:\[\]\}/)});
`;
fs.writeFileSync(testPath,test);
