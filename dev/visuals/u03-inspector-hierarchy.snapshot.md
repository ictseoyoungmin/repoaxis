# U03.3 Inspector hierarchy visual evidence

Visual capture was produced from branch commit `65cbbad980c37f13983646a2393a1d656787f721` with the actual Repoaxis viewer at 1600×1000.

Observed Inspector Overview order:

1. Current state
2. Indexed facts
3. Last file commit
4. Agent note
5. Repository link

The capture artifact is intentionally not committed as a binary. It was generated through GitHub Actions run `33622379095`, artifact `u03-inspector-hierarchy`, alongside a self-contained Repoaxis HTML snapshot.

The temporary capture workflow and Playwright helper were removed before PR creation.
